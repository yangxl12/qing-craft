# 2026-08-29

## 接入微信开发者工具 MCP（WorkBuddy）

- 新建 `~/.workbuddy/mcp.json`，添加 `wechat-devtools`（command: `uvx wechat-devtools-mcp`，v0.9.16）。
- 与 codebuddy（`~/.codebuddy/mcp.json`）、codex（`~/.codex/config.toml`）配置一致，可三端共用：
  MCP 是 stdio 子进程，各客户端各自 spawn 实例，互不干扰；共用的是底层微信开发者工具 IDE，建议串行操作。
- 必需环境变量：`WECHAT_DEVTOOLS_CLI`（`D:\微信web开发者工具\cli.bat`）、`WECHAT_PROJECT_PATH`。
- 注意：全局配置里 `WECHAT_PROJECT_PATH` 写死为 `D:\my-project\qing-craft`；
  codex 那份指向 `D:\my-project\digital-ceramics`。切换小程序项目时需改此变量。
- 生效方式：需在 WorkBuddy 连接器管理页的自定义连接器入口点击「信任」后启用。

## 创作台底栏改版（工序流程条下移）

- 需求：七个工序页（除高温烧制/低温烤花两个窑烧页）统一改底栏——顶部 7 字流程条移到底栏正中、删掉底栏中间标题、左右按钮只留箭头图标。
- 实现要点：
  - `studio.wxml` 用 `<template name="stageProgress">` 复用流程条，在 stage 0/1/2/4/6 的底栏中间各引用一次（`data` 用显式 `key: value` 写法）。
  - 原 `wx:else` 的 tray 分支被 stage 3/5/6 共用，现拆成 `wx:elif="{{stageIndex===6}}"`（新版）+ `wx:else`（3/5 保持原样，且窑烧时 `.kiln-active .tray` 本就隐藏）。
  - 样式：`.stage-progress` 由整行 grid 改为固定列宽 `repeat(7, 40rpx)` + 居中，去掉独立背景；左右按钮收成 88rpx 方形图标按钮。
- 坑：studio 页面（webgl canvas）用 `wechat_screenshot` 会 automator 超时，改完只能靠 `wechat_build(compile)` + `tests/*.cjs` 验证，无法截图。
